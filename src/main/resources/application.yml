spring:
  profiles:
    active: dev

info:
  application:
    name: clinical application
    description: clincicalapp developed on The Spring Framework (back) and React Typescript (front).
    author: Mohamed
    version: "@project.version@"
